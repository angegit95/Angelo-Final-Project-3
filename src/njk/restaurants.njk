{% extends '_templates/base.njk' %}

{% block content %}
<article class="container">
  <div class="offset-md-2 col-md-8">
    <h1>Restaurants</h1>
    <p>From SoCo to Rainey Street to the Domain, Austin has some of the best restaurants around. 
     No matter what you're craving, Cap City has got you covered. Browse through the best of Austin's grub.</p>
  </div>
  <div class="row">
    {% for restaurants in restaurants.restaurants %}
    <div class="col-sm-6 col-lg-4">
      <div class="card text-black bg-light mb-4" style="max width: 18rem:">
            <h5 class="card-header">{{ restaurants.name }}</h5>
        <div class="card-body">
          <img src="img/restaurants/{{ restaurants.image }}" alt="" class="img-fluid">
          <h6 class="card-captain">{{ restaurants.address }}</h6>
          <p class="card-text">{{ restaurants.category }}</p>
        </div>
      </div>
    </div>
    {% endfor %}
  </div>
</article>
{% endblock %}